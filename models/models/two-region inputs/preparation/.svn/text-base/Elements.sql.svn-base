set search_path to luc, public, pg_catalog;


create view "Elements" as
  select distinct
      "element codes" as "Element Code"
    , "element" as "Element"
    from "Raw Production - Crops"
  order by 2
;
